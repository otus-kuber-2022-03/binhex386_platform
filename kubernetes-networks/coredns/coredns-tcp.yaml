apiVersion: v1
kind: Service
metadata:
  name: dns-service-tcp
  namespace: kube-system
spec:
  type: LoadBalancer
  ports:
    - name: dnstcp
      protocol: TCP
      port: 53
      targetPort: 53
  selector:
    k8s-app: kube-dns
